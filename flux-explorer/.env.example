# Flux Blockchain Explorer - Environment Configuration
# Copy this file to .env.local and configure as needed

# ============================================================================
# Blockbook API Configuration
# ============================================================================

# Public Blockbook API endpoint (fallback)
NEXT_PUBLIC_BLOCKBOOK_API_URL=https://blockbookflux.app.runonflux.io/api/v2

# Local Blockbook API endpoint (optional - for better performance)
# Uncomment and configure if you have a local Blockbook instance
# NEXT_PUBLIC_LOCAL_BLOCKBOOK_API_URL=http://localhost:9158/api/v2

# For server-side API routes (internal Flux component communication)
# BLOCKBOOK_API_URL=http://fluxblockbook_yourappname:9158/api/v2

# ============================================================================
# API Mode Configuration
# ============================================================================

# API_MODE: Controls how the explorer connects to Blockbook
#   - 'auto' (default): Automatically detects and switches between local/public
#   - 'local': Force local Blockbook (fast polling, no rate limits)
#   - 'public': Force public Blockbook (conservative polling, rate-limited)
NEXT_PUBLIC_API_MODE=auto

# ============================================================================
# Advanced API Configuration (Optional)
# ============================================================================
# Override specific settings - useful for fine-tuning performance
# Leave commented to use defaults based on API_MODE

# Request timeout in milliseconds
# Local default: 10000 (10s), Public default: 30000 (30s)
# API_TIMEOUT=10000

# Number of retry attempts for failed requests
# Local default: 1, Public default: 3
# API_RETRY_LIMIT=2

# Polling interval for real-time updates (milliseconds)
# Local default: 3000 (3s), Public default: 30000 (30s)
# API_REFETCH_INTERVAL=5000

# Scanner batch size (blocks processed per batch)
# Local default: 500, Public default: 50
# API_BATCH_SIZE=100

# Scanner throttle delay (milliseconds between batches)
# Local default: 100ms, Public default: 2000ms
# API_THROTTLE_DELAY=1000
